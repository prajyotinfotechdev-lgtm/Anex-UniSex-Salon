import { InspirationFeed } from '@/modules/inspiration/components/inspiration-feed';

export const metadata = {
  title: 'Inspiration | Anex',
};

export default function InspirationPage() {
  return <InspirationFeed />;
}
